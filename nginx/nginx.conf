server {
    listen 80;
    server_name tbit-msp.kro.kr;

    # Let's Encrypt 인증용 (certbot)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # HTTP → HTTPS 리다이렉트 (인증서 발급 후 아래 주석 해제)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # 임시: 인증서 발급 전까지 직접 프록시
    location / {
        proxy_pass         http://app:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# HTTPS — 도메인 + 인증서 발급 후 아래 주석 해제
# server {
#     listen 443 ssl;
#     server_name tbit-msp.kro.kr;
#
#     ssl_certificate     /etc/letsencrypt/live/tbit-msp.kro.kr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tbit-msp.kro.kr/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass         http://app:8000;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
