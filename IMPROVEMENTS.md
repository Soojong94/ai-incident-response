# 개선 사항 목록

## 🔴 우선순위 높음

### 1. 폴러 재시작 시 다운타임 이벤트 누락
**문제:** 중앙 서버가 다운됐다가 재시작하면 `_last_poll_ms`가 리셋되어 최근 2분만 뒤돌아봄. 다운된 사이의 Cloud Insight 이벤트 누락.  
**조치:** 재시작 시 DB에서 마지막 처리된 인시던트의 `alarm_time`을 읽어 폴링 시작 시간으로 복원.
```python
last = db.query(Incident).order_by(Incident.alarm_time.desc()).first()
self._last_poll_ms = last.alarm_time.timestamp() * 1000 if last else now - 120_000
```

### 2. AI 분석 큐 대기 시간 무제한
**문제:** `Semaphore(1)`로 순차 처리 시 알람 6개 동시 발생이면 마지막 건은 최대 6분 대기. PRD 목표 "5분 이내 분석 완료" 초과 가능.  
**조치:** `Semaphore(2)`로 상향 + `asyncio.wait_for(timeout=120)`으로 분석당 최대 대기 시간 제한.

---

## 🟡 우선순위 중간

### 3. AI 분석 실패 시 재시도 없음
**문제:** Timely API 일시 오류 시 `ai_failed` 상태로 종료. 운영자가 수동 재처리 필요.  
**조치:** 1회 재시도 추가 (지수 백오프 불필요, 단순 1회로 충분).

### 4. 재시작 시 중복 이벤트 처리 (에러 로그 누락)
**문제:** `_seen_event_ids` 메모리 손실로 재시작 후 같은 이벤트 재처리 시도. `alarm_id UNIQUE` 제약으로 실제 중복 저장은 막히지만 에러가 조용히 묻힘.  
**조치:** `IntegrityError` 캐치 후 "이미 처리된 이벤트" 로그 출력.

---

## 🟢 우선순위 낮음 (프로덕션 전환 시)

### 5. SQLite → MySQL 전환
**문제:** SQLite는 파일 단위 쓰기 락으로 고부하 시 `SQLITE_BUSY` 가능성.  
**조치:** `DATABASE_URL` 환경변수만 변경하면 전환 가능 (이미 준비됨).

### 6. 다중 고객사 NCP 계정 관리
**문제:** 현재 `.env` 1세트만 지원. MSP 다계정 운영 시 부적합.  
**조치:** DB에 고객사 테이블 추가, 폴러를 고객사별로 인스턴스화. SA가 고객사별로 구성.

### 7. 수평 확장 (멀티 인스턴스)
**문제:** 서버 2대 이상 시 동일 이벤트 중복 폴링 → 중복 인시던트 생성 가능.  
**조치:** Redis 기반 분산 락 또는 DB 기반 리더 선출 필요. MVP 범위 외.

---

## 의도적으로 제외한 항목

- **자동 조치 (Auto Scaling, 서비스 재시작):** 사람이 직접 확인 후 조치. 자동화 미구현 유지.
- **Mock 로그 신뢰성:** 프로덕션에서 CLA 연동 시 자동 해결.
- **Slack 알림:** Phase 2 예정.
